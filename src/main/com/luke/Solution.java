package com.luke;

class Solution {
    public static void main(String[] args) {

    }
    // 数组 left,right
    // right的前缀和，preSum[right + 1] = preSum[right] + nums[right]
    // left-1的前缀和，preSum[left] = preSum[left-1] + nums[left-1]
}
